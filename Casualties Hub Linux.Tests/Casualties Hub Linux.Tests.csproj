<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Covers the logic that cannot be verified by clicking: path containment guards,
    Valve KeyValues parsing, and backup/restore file handling. These are the parts where a bug is
    silent or destructive rather than visible.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Casualties Hub Linux\Casualties Hub Linux.csproj" />
  </ItemGroup>

</Project>
